import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export default function Home() {
  // const dc = document.cookie.indexOf("token")
  return (
    <><div>xd</div>
    </>
    
  )
}
